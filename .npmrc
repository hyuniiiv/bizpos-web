# Next.js standalone + Electron 패키징 호환을 위해 pnpm을 hoisted(npm/yarn flat)로 강제.
# 기본 isolated 모드는 .next/standalone/node_modules/next 가 외부 .pnpm store를 가리키는
# 심볼릭 링크를 남겨 electron-builder 패키징 후 require('next') 실패를 유발한다.
node-linker=hoisted
